<div id="full-blue">
  <div class="container">
    <div class="intro-text">
      <div class="intro-heading">Nuevo registro</div>
    </div>
  </div>
</div>

</div>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <br>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.text_field :username, autofocus: true, :placeholder => "Usuario", :class => "form-control" %>
        </div>
        <div class="form-group">
          <%= f.email_field :email, :placeholder => "Correo Electrónico", :class => "form-control" %>
        </div>
        <div class="form-group">
          <%= f.password_field :password, autocomplete: "off", :placeholder => "Contraseña (#{@minimum_password_length} carácteres min)", :class => "form-control"  %>
        </div>

        <div class="form-group">
          <%= f.password_field :password_confirmation, autocomplete: "off", :placeholder => "Confirma Contraseña", :class => "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "Enviar", :class => "btn btn-xl btn-inverse  " %>
        </div>
      <% end %>
      <br>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
