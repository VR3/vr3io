<div id="full-blue">
  <div class="container">
    <div class="intro-text">
      <div class="intro-heading">Nuevo registro</div>
    </div>
  </div>
</div>

</div>
<div class="container">
  <div class="row text-center">
    <div class="col-md-6">
      <br>
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group">
          <%= f.email_field :email, autofocus: true, :placeholder => "Correo Electrónico", :class => "form-control" %>
        </div>

        <div class="form-group">

          <%= f.password_field :password, autocomplete: "off", :placeholder => "Contraseña (#{@minimum_password_length} carácteres min)", :class => "form-control"  %>
        </div>

        <div class="form-group">
          <%= f.password_field :password_confirmation, autocomplete: "off", :placeholder => "Confirma Contraseña", :class => "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "Enviar", :class => "btn-xl btn-primary" %>
        </div>
      <% end %>
      <br>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
